name: login_page
description: A new Flutter project.


publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  font_awesome_flutter: ^8.12.0
  firebase_auth: ^3.1.0
  cupertino_icons: ^1.0.2
  firebase_core: ^1.7.0
  google_sign_in: ^5.0.1
  intl_phone_number_input: ^0.2.0
  cloud_firestore: ^1.0.1
  firebase_storage: ^10.0.5
  file_picker: ^4.1.0
  flutter_custom_tabs: ^1.0.0
  path: ^1.8.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  
  uses-material-design: true
  
  assets:
    - assets/